export class FileService {
  async processFile(fileBuffer: Buffer, fileName: string): Promise<void> {
    // TODO: Extract text, chunk, generate embeddings, store in DB
    // Placeholder implementation
    return;
  }
}
